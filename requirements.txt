# Python 依赖（pip 安装）。
# 系统级依赖（非 pip，按需用系统包管理器安装）：
#   - TeX 发行版（pdflatex/xelatex）—— 编译 PDF 必需，见 README
#   - bubblewrap —— 可选，仅 codex 后端沙盒会用；不装则 codex 用内置的
#     Debian/Ubuntu/WSL: sudo apt install bubblewrap
beautifulsoup4==4.13.4
pylatexenc==2.10
PyYAML==6.0.2
regex==2024.11.6
Requests==2.32.4
tiktoken==0.9.0
toml==0.10.2
tqdm==4.67.1
aiohttp==3.11.16
async-lru==2.0.5
async-timeout==5.0.1
pandas==2.2.3
streamlit==1.44.1
datetime
